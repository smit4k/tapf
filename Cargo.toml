[package]
name = "tapf"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
thiserror = "1.0"

[features]
default = []
timestamps = ["dep:chrono"]

[dependencies.chrono]
version = "0.4"
optional = true
